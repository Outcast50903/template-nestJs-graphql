// TODO: Get roles from database?

export enum Roles {
  USER_ROLE = 'USER_ROLE',
  ADMIN_ROLE = 'ADMIN_ROLE',
}

export enum Actions {
  Aggregate = 'aggregate',
  Manage = 'manage',
  Create = 'create',
  Read = 'read',
  Update = 'update',
  Delete = 'delete',
}
